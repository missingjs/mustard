#include "common/matrix2.h"
using namespace mustard;

typedef matrix::common_matrix<double> mx_t;

void optimal_search_tree(int N, const std::vector<std::string> & words, const std::vector<double> & p);
