// @brief: 设有一个双链表h，每个结点中除有prev,data,和next 3个域外，还有一个访问频度哉freq，在链表被起用之前，每个结点中的freq域值均被初始化为0，每当进行LocateNode(h,x)运算时，令元素值为x的结点中freq域的值加1，并调整表中结点的次序，使其按访问频度的递减序列排列，以便使频繁访问的结点总是靠近表头。编写程序实现LocateNode(h,x)算法
